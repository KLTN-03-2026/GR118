# Fix permissions issues on Vercel and improve build reliability
unsafe-perm=true
fetch-timeout=600000
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
legacy-peer-deps=true
node-linker=hoisted
