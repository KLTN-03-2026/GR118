{
  "name": "issue-reporting-system",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "build:frontend": "cd Frontend && npm install --no-optional --legacy-peer-deps && npm run build"
  }
}
